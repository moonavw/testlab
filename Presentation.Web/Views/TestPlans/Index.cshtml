@model IEnumerable<TestLab.Domain.TestPlan>

@{
    ViewBag.Title = "Test Plans";
    TestLab.Domain.TestProject project = ViewBag.Project;
}

<h2>Test Plans</h2>
<p>
    @Html.ActionLink("Create New", "New", "TestPlans", new { testprojectId = project.Id }, new { @class = "btn btn-default" })
</p>

@Html.Grid(Model.OrderByDescending(z=>z.Id)).Columns(c =>
{
    c.Add()
     .Encoded(false)
     .Sanitized(false)
     .SetWidth(30)
     .RenderValueAs(d =>
                        Html.ActionLink("Open", "Show", "TestPlans", new {id = d.Id, testprojectId = project.Id}, new {@class = "btn btn-default"})
        );
    
    c.Add(o => o.Id)
     .SortInitialDirection(GridMvc.Sorting.GridSortDirection.Descending);

    c.Add(o => o.Name);

    c.Add(o => o.Cases.Count);
    
}).Sortable().Filterable().WithMultipleFilters().WithPaging(20)
