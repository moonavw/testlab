@model TestPlan

@{
    ViewBag.Title = "Test Plan: " + Model.Name;
}

<p><a href="@Url.Action("Edit")" class="btn btn-default">Edit</a></p>

<div>

    @Html.Partial("_Audit", Model)

</div>

<div>
    <h2 class="sub-header">Test Cases</h2>
    @Html.Partial("../TestProjects/_TestCases", Model.Cases.Actives())
</div>