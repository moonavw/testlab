@model TestLab.Domain.TestProject

@{
    ViewBag.Title = "TestProject";
}

<div>
    <h2>TestProject</h2>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>

@Html.Partial("_ShowRepo", Model.Repo)
@Html.Partial("_ShowSrc", Model.Src)
@Html.Partial("_ShowBin", Model.Bin)
@Html.Partial("_ShowAudit", Model)

<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }, new { @class = "btn btn-default" })
    @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-default" })

    @Html.ActionLink("TestPlans", "Index", "TestPlans", new { testprojectId = Model.Id }, new { @class = "btn btn-default" })
    @Html.ActionLink("TestCases", "Index", "TestCases", new { testprojectId = Model.Id }, new { @class = "btn btn-default" })
</p>
@using (Html.BeginForm("Build", "TestProjects", new { id = Model.Id }))
{
    <input type="submit" value="Build" class="btn btn-success" />
}
